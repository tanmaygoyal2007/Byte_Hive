{
  "private": true,
  "scripts": {
    "dev": "node scripts/dev-stack.mjs",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": "cross-env FIREBASE_AUTH_EMULATOR_HOST=127.0.0.1:19199 FIRESTORE_EMULATOR_HOST=127.0.0.1:18180 FIREBASE_PROJECT_ID=demo-bytehive npm --prefix backend run dev",
    "dev:firebase": "firebase emulators:start --only auth,firestore"
  },
  "dependencies": {
    "react-icons": "^5.6.0",
    "react-router-dom": "^7.13.1"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "cross-env": "^10.1.0",
    "firebase-tools": "^14.20.0"
  }
}
